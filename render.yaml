services:
  - type: web
    name: stock-algo-trading
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/main.py --server.address=0.0.0.0 --server.port=$PORT
    healthCheckPath: /_stcore/health
